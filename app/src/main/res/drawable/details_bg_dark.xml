<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <!-- create gradient you want to use with the angle you want to use -->
        <shape android:shape="rectangle">
            <size
                android:width="163dp"
                android:height="146dp" />
            <corners android:radius="55dp">

            </corners>

            <gradient
                android:angle="270"
                android:startColor="@color/dark_start_border_grad"
                android:endColor="@color/dark_end_border_grad"
                />

        </shape>
    </item>
    <!-- create the stroke for top, left, bottom and right with the dp you want -->
    <item
        android:bottom="2.2dp"
        android:left="2.2dp"
        android:right="2.2dp"
        android:top="2.2dp">
        <shape android:shape="rectangle">
            <!-- fill the inside in the color you want (could also be a gradient again if you want to, just change solid to gradient and enter angle, start, maybe center, and end color) -->
            <gradient
                android:angle="315"
                android:startColor="@color/dark_start_default_grad"
                android:endColor="@color/dark_end_default_grad"/>

            <corners android:radius="55dp" />
        </shape>
    </item>

</layer-list>